specVersion: "v1"
serviceId: "pvpchat"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "pvpchat-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
